- [Data Agent future vision](data_agent_future_vision.md) — planned: central-DB big-data queries replace the in-memory simulated gateway; current `SimulatedDataGateway` is a stand-in
- [Workflow preferences](workflow_preferences.md) — prefer direct Edit/Bash execution over Task-tool subagent dispatches; subagents trigger permission prompts that create friction
- [SafeChain dual environment](safechain_dual_environment.md) — private/prod uses safechain (no native tool-calls, single combined message); dev uses OpenAI API + openai-agents SDK directly. v2's SDK-based wiring is dev-only without an adapter.
- [Commit only when explicitly asked](feedback_commit_only_when_asked.md) — NEVER auto-commit or auto-push. Wait for the user to say "commit" / "push" / similar in the CURRENT turn. Even after a feature passes tests, end the turn with an offer ("Want me to commit?"), not a commit.
- [Date-format handling is load-bearing](project_date_format_sensitivity.md) — `_date_key` parser failures cascade into specialist "no parseable values" findings; recurring across private/dev format drift. Extend per format, quote samples in findings.
- [Orchestration flow UX preferences](feedback_orchestration_flow_ux.md) — frontend: panels must NOT snap to streaming turn when user clicked a prior one; chat agent at same y-position in compact & expanded; flow grows incrementally without rearranging already-rendered parts.
- [Wire frontend with backend in same change](feedback_wire_frontend_with_backend.md) — when backend changes touch SSE events / chart payloads / KP shape, always update `My Drive/Projs/CaseReviewChat` in the same turn; backend-only diffs silently break the UI with no compile/test failure.
- [Alternate paths must replay full SSE](feedback_alternate_paths_must_replay_full_sse.md) — every branch that emits `final` (cache hit, fallback, retry, error short-circuit) MUST also emit `team_plan` + `agent_started` + `agent_completed` + `chart`; otherwise the UI shows an answer with empty trace panels and the user reads it as a silent failure.
- [Performance targets](feedback_performance_targets.md) — hard wall-clock budgets the user has named: screen-rejection < 5s, single-specialist answer ≤ 20s. Exceeding either is a regression to root-cause, not a tolerance.
- [Plots preference](feedback_plots_preference.md) — prefer vertical `bar` over horizontal `share`; switch only when long labels physically don't fit. Always sort points by a meaningful dimension (temporal / rank / category-natural), never tool-return order.
- [Transaction vs monthly tables](transaction_vs_monthly_tables.md) — two grains: monthly for trends, transaction (model_scores_transaction / score_drivers_transaction) for per-txn & approve-deny questions; filter day-grain trans_dt by default.
- [SafeChain model calling + thread occupation](safechain_async_and_thread_occupation.md) — "stuck at team construction" / "input not captured" = hung safechain calls leaking threads. Correct API: build via `await amodel(model_id)` (async factory, cache, rebuild on 401), then sync `chain.invoke` (`prompt | model | StrOutputParser()`) in a dedicated thread pool + per-call `asyncio.wait_for`. Do NOT use `chain.ainvoke`. Don't restore f15d6b9's reverted thread pool.
- [Ask prod for safechain unknowns](feedback_safechain_ask_prod_questions.md) — safechain is prod-only (uninstallable in dev). For safechain work, raise concrete clarifying questions the user can run in the private env BEFORE designing the fix; don't guess its API/async/threading behavior.
