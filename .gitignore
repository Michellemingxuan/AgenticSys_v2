.DS_Store
__pycache__/
*.pyc
.env
logs/
context/
# data_tables/ holds per-case CSVs (real or synthetic export). Keep the
# top-level README and the simulated/ + real/ subfolder READMEs tracked, but
# never commit actual CSV contents — they can carry sensitive PII.
data_tables/*/*/
!data_tables/simulated/README.md
!data_tables/real/README.md
# Consistency / load-test result CSVs — generated per run, may echo case data.
tests/test_consistency/results/
.superpowers/
venv/
.venv/
.claude/
reports/
# Private verified snapshots — never commit (local versioning only)
.catalog_verified/
# Provenance file is private/prod-only — never commit it
config/data_profiles/.provenance.json
